/*
 * This is the overriding styles
 */

const style = (theme) => ({
  root: {
    height: "100%",
    // paddingTop: theme.mixins.toolbar.minHeight,
  },
  list: {
    padding: 0,
  },
});

export default style;
